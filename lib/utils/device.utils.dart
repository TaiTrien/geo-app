import 'package:flutter/material.dart';

class DeviceUtils {
  static deviceHeight(context) => MediaQuery.of(context).size.height;
  static deviceWidth(context) => MediaQuery.of(context).size.width;
}
